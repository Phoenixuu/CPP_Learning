#include <iostream>
using namespace std;
#include<math.h>

int main() {
	
	int n; cin >> n;
	int arr[n] = {0};
	int a;
	for (int i = 0; i < n; i++){
		cin >> a;
		arr[i] += sqrt(a);
	} 
    for (int i = 0; i < n; i++){
        cout << "#" << (i+1) << " " << arr[i] << endl;
    }
	
	return 0;
}
